<form method="post" action="{{ path('app_evidence_delete', {'id': evidence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evidence.id) }}">
    <button class="btn">Delete</button>
</form>
