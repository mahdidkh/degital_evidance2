<form method="post" action="{{ path('app_evidance_delete', {'id': evidance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evidance.id) }}">
    <button class="btn">Delete</button>
</form>
